# Security Policy

## 支持范围

zeroServer 当前处于 `0.x` 开发预览阶段，尚未发布稳定版本。安全修复优先落在默认分支 `main`；旧提交和未发布快照不提供长期安全维护承诺。

当前公开能力不等同于完整生产安全网关。TLS/WAF/DDoS 防护、真实身份系统、完整 GM RBAC/审批、生产日志落地和集群级安全仍需要部署方独立评估与补齐。

## 私密报告漏洞

请使用仓库的 [GitHub Private Vulnerability Reporting](https://github.com/yixunfei/zero-server/security/advisories/new) 私密提交安全报告。不要创建包含可利用细节、真实凭据或玩家数据的公开 Issue。

报告建议包含：

- 受影响模块、分支或版本。
- 威胁模型和前置条件。
- 最小复现步骤或概念验证。
- 对机密性、完整性、可用性和玩家状态的影响。
- 可行的缓解或修复建议。

维护者会尽快确认报告、评估影响，并在修复可用后协调披露。请在获得同意前不要公开利用细节。

## 重点安全边界

- 协议身份、完整性、防重放和输入预算。
- Netty 连接生命周期、鉴权、限流和背压。
- Kafka RPC 伪造、超时、幂等、回复路由和敏感配置。
- Nacos 服务发现元数据与注册身份。
- MongoDB、Redis、PostgreSQL 凭据和数据边界。
- GM RBAC、IP 白名单、审批、dry-run 和审计归因。
- 热更上传、签名、ClassLoader 和集群同步。
- 日志注入、敏感字段泄漏、高基数指标和 TraceId 传播。

## 部署方责任

- 不把示例配置或测试凭据用于真实环境。
- 通过密钥管理系统注入凭据，不写入源码、镜像或仓库。
- 在公网入口前配置 TLS、访问控制、速率限制和网络隔离。
- 对外部中间件启用认证、授权、传输加密、备份和审计。
- 在上线前完成威胁建模、依赖扫描、压测、故障演练和安全评审。

<!-- zero-security-reporting=private-channel -->
