# zeroServer 发布检查单

本检查单用于维护者准备 GitHub Release、Maven 制品或生产部署。它是证据清单，不是自动发布授权；每一项都应记录“通过 / 失败 / 未运行 / 不适用”和可复核证据。

## 1. 发布元数据

- 发布版本：`<version>`
- 基线提交：`<commit SHA>`
- 目标分支：`<branch>`
- 发布负责人：`<maintainer>`
- 复核人：`<reviewer>`
- 计划窗口与时区：`<time>`
- 发布渠道 / 制品仓库：`<GitHub / Maven repository / deployment>`
- 当前结论：`未开始 / 进行中 / 阻断 / 可发布 / 已发布 / 已回滚`

建议在 Pull Request、GitHub Issue 或 Release 草稿中保存本检查单的具体实例，附上 CI、构建日志、制品校验值和手工验证结果。

## 2. 范围与变更说明

- [ ] 已列出本版本包含的模块、用户可见能力、修复项和非目标。
- [ ] 已标记新增、变更、性能、安全、弃用和破坏性变化。
- [ ] `CHANGELOG.md` 的 `Unreleased` 已审阅并整理为版本说明。
- [ ] 行为变更已同步产品、架构、模块、协议、线程、RPC、数据、日志、GM 或热更文档。
- [ ] 模块、入口、依赖方向或常见修改位置变化已同步 `docs/module-map.md`。
- [ ] 公开 API、协议、线程模型、RPC、存储、权限或日志字段变更有已评审的 Design Proposal。
- [ ] Release notes 没有泄漏未公开漏洞细节、凭据、本机路径或内部运维信息。

证据：`<Pull Request / Issue / 文档链接>`

## 3. 兼容性与迁移

- [ ] 已明确 Java、Maven、操作系统、中间件和客户端兼容范围。
- [ ] 已检查 Maven 坐标、BOM、模块依赖、公共 API / SPI、注解、协议 DSL 和代码生成影响。
- [ ] 已检查协议 ID、wire format、RPC 语义、存储格式、缓存 key、日志字段、ErrorCode 和配置兼容性。
- [ ] 破坏性变更已有版本化迁移文档，例如 `docs/migrations/<version>.zh-CN.md`。
- [ ] 已说明弃用周期、灰度、双读 / 双写或向后兼容策略；不适用时写明原因。
- [ ] 已给出升级前备份、迁移步骤、验证点、失败处置和回滚触发条件。
- [ ] 生成器变化已验证 Java、C#、TypeScript 和其他受影响客户端产物。

证据：`<迁移说明链接>`

## 4. 代码、安全与许可证

- [ ] 没有提交密码、token、私钥、签名材料、真实连接串或本地开发信息。
- [ ] 已检查 GM、RBAC、IP 白名单、审批、dry-run、审计和安全日志影响。
- [ ] 已检查第三方依赖漏洞、许可证和制品来源。
- [ ] 已按 `SECURITY.md` 私下处理未公开漏洞。
- [ ] 异常未被生吞；对外失败和错误日志使用明确 ErrorCode。
- [ ] TraceId、关键日志、指标、告警和审计证据覆盖本次变更。
- [ ] 发布文件保持 MIT License，新增源码和资源具备可发布权利。

证据：`<安全审阅、依赖扫描、许可证报告>`

## 5. 分层验证

上层结果不能替代下层或外部环境验证。真实中间件测试只能在隔离环境中显式运行。

| 层级 | 推荐命令或证据 | 结论 | 证据位置 |
| --- | --- | --- | --- |
| Java / Maven 环境 | `java -version`、`mvn -version` | `<填写>` | `<填写>` |
| 仓库入口 | `java scripts/ZeroLocalDoctor.java` | `<填写>` | `<填写>` |
| 阶段 0 本地闭环 | `java scripts/ZeroStage0Acceptance.java --level full` | `<填写>` | `target/stage0-acceptance/logs` |
| 架构边界 | `java scripts/ZeroArchitectureGuard.java` | `<填写>` | `<填写>` |
| Maven validate | `mvn -B -ntp -DskipTests validate` | `<填写>` | `<填写>` |
| 默认测试 | `mvn -B -ntp test` | `<填写>` | `<填写>` |
| 质量门禁 | `mvn -B -ntp -Pquality verify` | `<填写>` | `<填写>` |
| 本地集成测试 | `mvn -B -ntp -Pintegration-tests verify` | `<填写>` | `<填写>` |
| 外部中间件测试 | `mvn -B -ntp -Pexternal-tests verify` | `<填写>` | `<填写环境>` |
| 示例 | 按 `README.md` 逐个执行 test / run | `<填写>` | `<填写>` |
| 脚手架 | `java scripts/VerifyLocalScaffolds.java --outputDir target/scaffold-verify` | `<填写>` | `<填写>` |
| 性能 | JMH、协议比较、压测、背压与长稳证据 | `<填写>` | `<填写>` |
| 手工验收 | 安装、升级、兼容、回滚、安全与运营路径 | `<填写>` | `<填写>` |

- [ ] 所有失败已处理或明确阻断发布。
- [ ] 所有未运行项均记录原因、影响和剩余风险。
- [ ] 外部环境记录组件版本、端口、配置来源、数据目录和清理方式，但不记录秘密值。
- [ ] 性能结果包含硬件、OS、JDK / JVM 参数、样本、预热、测量和原始输出位置。
- [ ] 没有把 microbenchmark 解释为生产吞吐、容量或 SLA 承诺。

## 6. 制品与可复现性

- [ ] 版本号不再含 `SNAPSHOT`，或已明确本次只发布预览源码而不发布正式制品。
- [ ] 构建使用明确的 Java 21、Maven 版本和可信依赖源。
- [ ] BOM、parent、starter、Adapter、示例和生成器版本一致。
- [ ] 发布制品清单、大小、SHA-256、来源提交和构建日志已记录。
- [ ] 如使用签名，签名身份、密钥保管和验证命令已确认；秘密材料不进入仓库。
- [ ] 已在干净环境验证克隆、构建、安装和最小启动路径。
- [ ] 源码包不含 `target/`、日志、IDE 配置、本地凭据或调试转储。

证据：`<制品清单与校验值>`

## 7. Git 与 GitHub

- [ ] 目标提交已合入受保护的默认分支。
- [ ] 工作流在目标提交上通过，必需检查没有被跳过。
- [ ] Tag 与版本一致，指向正确提交；如需签名，签名可验证。
- [ ] Release 标题、说明、迁移提示、已知限制和制品完整。
- [ ] README、MIT License、贡献指南、安全策略和行为准则可访问。
- [ ] Issue / Pull Request 模板、Dependabot 与仓库安全设置符合维护策略。
- [ ] 发布页面没有内部链接、失效链接或不可访问的本地文件引用。

批准记录：`<维护者复核来源>`

## 8. 部署、回滚与恢复

- [ ] 已定义发布前备份与恢复检查。
- [ ] 已明确代码、配置、协议、数据、缓存和中间件的回滚边界。
- [ ] 数据或协议不可逆时，已提供向前修复、双版本兼容或维护窗口方案。
- [ ] 已定义触发回滚的错误率、尾延迟、容量、安全和业务指标。
- [ ] 已明确回滚负责人、命令、顺序、预计时长和验证点。
- [ ] 已完成与风险相称的回滚演练；未演练时记录原因和替代措施。

回滚方案：`<链接>`

## 9. 发布后观察

- [ ] 已确认服务健康、日志、TraceId、ErrorCode、指标、告警和审计链路。
- [ ] 已观察连接、Actor 队列、RPC pending、缓存、落库、事件死信和调度任务积压。
- [ ] 已验证关键用户流程、GM / dry-run、数据一致性和客户端兼容性。
- [ ] 已记录观察窗口、值班人、已知问题和是否保持回滚窗口。
- [ ] 已更新 Changelog、Release notes 和最终验证结论。
- [ ] 发布后问题已关联到 GitHub Issue，并标明严重度和处置负责人。

最终结论：`<成功 / 继续观察 / 回滚>`

## 10. 阻断规则

出现以下任一情况时不得标记“可发布”：

- 必需的公共契约变更没有设计评审，或实际改动超出已评审范围。
- 默认测试、必需 quality 或必需外部验证失败且没有处置结论。
- 破坏性变更缺少迁移、兼容或回滚说明。
- 凭据、签名、制品目标、操作者或审计证据不明确。
- 已知数据损坏、安全漏洞、协议不兼容或不可接受的性能回归未被处置。
- 制品无法从声明的提交和工具版本复现。

本模板本身不授权 tag、发布制品或部署；执行外部发布动作前仍需维护者明确批准。
